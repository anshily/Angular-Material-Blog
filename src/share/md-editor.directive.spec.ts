import { MdEditorDirective } from './md-editor.directive';

describe('MdEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new MdEditorDirective();
    expect(directive).toBeTruthy();
  });
});
